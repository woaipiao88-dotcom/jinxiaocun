# Contributing

感谢你关注这个进销存项目。项目目标是保持部署简单、业务逻辑清晰，并尽量兼容局域网和旧设备环境。

## 开发原则

- 优先保持 Python + Flask + SQLite + 原生前端的轻量结构。
- 不引入前端构建链，除非有明确收益。
- 库存变化必须通过单据和库存流水完成，不直接手工改库存余额。
- 数据库、备份、真实客户或供应商资料不要提交到仓库。
- 提交功能时尽量补充手工测试步骤或自动化测试。

## 本地开发

```bash
python -m pip install -r requirements.txt
python app.py
```

访问：

```text
http://127.0.0.1:5001
```

## 提交 Pull Request

1. Fork 仓库并创建功能分支。
2. 保持改动聚焦，一次 PR 只解决一个主题。
3. 在 PR 描述中说明改动内容、影响范围和验证方式。
4. 如果改动库存、单据、作废、删除逻辑，请额外说明库存流水是否正确。

## Issue 建议

提交 Issue 时请包含：

- 运行环境：Windows 版本、Python 版本、浏览器
- 复现步骤
- 期望结果和实际结果
- 如有报错，请附上终端输出
